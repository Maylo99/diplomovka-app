<div class="container d-flex justify-content-center align-items-center vh-100">
  <div class="card p-4 rounded-4" style="width: 600px;">
    <div class="m-auto pt-3">
      <a href="/app/assets/builds" class="d-block mb-4 rounded">
        <%= image_tag("logo.jpeg", alt: 'Logo', width: 100, class: "rounded") %>
      </a>
    </div>
    <h1 class="text-center mb-4 text-primary">Neprišiel ti potvrdzujúci email?</h1>
    <span class="text-secondary text-center w-75 mx-auto h5">Netráp sa a zadaj svoj email a my ti pošleme správu s inštrukciami.</span>
    <hr class="w-75 mx-auto">
    <div class="card-body p-0">
      <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
    <div class="form-group mb-3 w-75 mx-auto">
          <%= f.label :email, "Email",class: "form-label" %>
          <%= f.email_field :email, autofocus: true, placeholder: "maylo@gmail.com", autocomplete: "email", class: "form-control" %>
          <%= render "shared/error_form_field", object: resource, field: :email %>
        </div>
        <div class="text-center">
          <%= f.submit "Pošli mi email", class: "btn btn-primary w-75 btn-lg mb-3" %>
        </div>
      <% end %>
      <div class=" mx-auto w-75 mt-3 mb-3">
        <div class="text-center">
          <span class="text-secondary">Už máš účet vytvorený?</span> <%= link_to "Prihlás sa", new_session_path(resource_name) %>
        </div>
      </div>
    </div>
  </div>
</div>